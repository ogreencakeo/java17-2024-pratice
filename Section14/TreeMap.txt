[ TreeMap ]
- 맵은 기본적으로 키(Key)와 값(Value)의 쌍으로 구성
- 트리맵하면 일단 정렬이 되어진 상태로 맵이 구성 ~> 이때, 기본적으로 키를 기준으로 정렬 ~> 오름차순
- 따라서, 키가 문자열(String) 타입이면 가나다순, 알파벳순 정렬
- 트리맵 요소에 대한 다양한 처리를 어떻게 하는지 아는게 중요 ~> 추가, 삭제, 출력, 수정 등 ... 
- Key : Value 쌍을 어떻게 구성할지는 고민이 필요 ~> 기본적으로 키를 기준으로 정렬이 되기 때문에
